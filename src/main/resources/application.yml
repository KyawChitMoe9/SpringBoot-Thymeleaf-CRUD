spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myweb_db
    username: kyawgyi
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  spring:
    web:
      resources:
        static-locations: classpath:/static/

  # Remove or comment out this configuration
  # mvc:
  #   static-path-pattern: /static/**